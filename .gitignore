# Working notes about the local machine — never published.
odd/

# Real routing rules live here; only the example is tracked.
config/router.json

# Local instance state
.engram/
*.log

# Local tooling artifacts
.atl/
.codegraph/

# Screenshots dropped in the repo root — this repository is public.
/*.png
/*.jpg

# Literal '$H' directories: an unexpanded variable in a test harness once made
# the installer create one inside the repository.
/$H/
